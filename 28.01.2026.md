lozan0.node.dhu

kobenhavn.s2.rpn.ch

ssh henochrjt@lozan0.node.dhu -oProxyCommand="ssh henochrjt@kobenhavn.s2.rpn.ch -A -i ~/.ssh/henochrjt@kobenhavn_rsa -W %h:%p" -i ~/.ssh/henochrjt@lozan0_rsa -A


# Activité 001

## Exercice 1

Après avoir ouvert une session de travail ssh sur le serveur pédagogique Linux, exécuter les commandes suivantes et expliquer simplement ce que font ces commandes.

```
apropos password | ça recherche dans les descriptions des pages de manuel toutes les commandes liées au mot-clé "password"
man passwd | ça affiche les informations et les paramètre de la commande "passwd"
man man | ça affiche le manuel du manuel entre autre comment utiliser le manuel
help | affiche l’aide des commandes internes du shell
```

## Exercice 2

Exécuter les commandes suivantes, rechercher la documentation et expliquer simplement ce que font ces commandes.

```
w | Afficher les utilisateurs présents sur le système et leur activité
who | ça affiche qui est connecté sur le terminal
whoami | ça affiche l'utilisateur actuel
users | ça affiches les utilisateurs qui sont actuellement connectés au host
tty | ça affiche au quel terminal je suis connecté
id | ça affiche mes permissions (user et groupe ID)
logname | ça affiche mon nom de login
date | ça affiche la date
hostname | affiche le nom du host
df | ça affiche un rapport de l'espace de stockage
clear | ça nettoire l'écran du terminal
pwd | ça affiche le directory dans lequel je travaille actuellement
echo $LOGNAME | ça affiche la variable $LOGNAME qui dans ce cas est mon nom d'utilisateur

echo $TERM 
printenv
ps
ps -a
ps -ax
ps -alx
ls
ls -a
ls -l
ls -al
history
```


J'ai supp le mdp de mes clés avec : ssh-keygen -p -f ~/.ssh/henochrjt@lozan0_rsa  / ssh-keygen -p -f ~/.ssh/henochrjt@kobenhavn_rsa 