```table-of-contents
```


# Notes UNIX - 04.02.2026

## Question en suspens

Quand je fais cette commande :
```
huguenindo@lozan:~$ cat < /usr/include/aliases.h  |  grep -n 'include'
22:#include <features.h>
24:#include <sys/types.h>
```
Chez moi le fichier aliases.h n'existe pas

---

## Fichiers de travaux maison

Concernant les motifs génériques, les filtres et les redirections :
- ![[779075.cast]]
- ![[779075.txt]]

---

## Notes de synthèse

- Un chemin absolu commence toujours par `/`
- `./` veut dire chemin relatif
- En Unix, le nom de fichier c'est son chemin
- Les accolades `{}` sont pour créer des séquences : `touch a{1..10}`
- Les crochets `[]` c'est pour les recherches : `ls -la *[1..10]`

---

# Activités, série 0002 - Gestion Fichier

## Objectifs

- Introduction aux commandes UNIX
- Gestion des répertoires et des fichiers

> - Résoudre chaque problème suivant en un minimum d’instructions. Généralement une instruction suffit!
> - Écrire la commande permettant de vérifier le résultat

## Exercices

### 1. Créer trois sous-répertoires

Créer trois sous-répertoires dans votre «home directory»: trav1, trav2 et trav3.

```bash
cd ~
mkdir trav{1..3}
```

### 2. Copier les fichiers .h (a-d)

Copier dans le répertoire trav1 tous les fichiers .h dont le nom commence par a, b, c ou d et qui se trouvent dans le répertoire /usr/include.

```bash
cp /usr/include/[a-d]*.h ~/trav1
```

### 3. Copier avec motif complexe

Copier dans le répertoire trav2 tous les fichiers dont le nom commence par 3 caractères quelconques suivis d'un caractère compris entre e et z et se terminant par .h. Les fichiers sources se trouvent dans le répertoire /usr/include.

```bash
cp /usr/include/???[e-z].h ~/trav2
```

### 4. Lister le contenu

Lister le contenu de ces répertoires.

```bash
ls -la trav[12]
```

### 5. Visualiser et changer de répertoire

Visualiser votre répertoire de travail courant et choisir trav3.

```bash
pwd && cd ~/trav3
```

### 6. Copier des fichiers spécifiques

Copier dans trav3 les fichiers a.out.h, crypt.h et math.h qui se trouvent dans l'un de vos répertoires.

```bash
cp ~/trav1/{a.out,crypt,math}.h ~/trav3
```

### 7. Renommer des fichiers

Renommer le fichier aliases.h du répertoire trav1 et math.h du répertoire trav3 en Aliases.H et Math.H.

```bash
mv ~/trav1/aliases.h ~/trav1/Aliases.H
mv ~/trav3/math.h ~/trav3/Math.H
```

### 8. Effacer avec motif

Effacer tous les fichiers dont le nom commence par cry et qui se trouvent dans le répertoire trav1.

```bash
rm ~/trav1/cry*
```

### 9. Supprimer un répertoire

Supprimer le répertoire trav3.

```bash
rm -rf ~/trav3
```

# Activités, série 0003 - Redirection et filtre

## Objectifs

- Redirections et filtres

> - Résoudre chaque problème suivant en un minimum d’instructions. Généralement une instruction suffit
> - Écrire la commande permettant de vérifier le résultat

## Exercices

### 1. Exercer les commandes de visualisation

Exercer les commandes cat, head, tail, less et more. Pour less exercer ses propres sous-commandes.

```bash
cat /usr/include/aliases.h
head /usr/inc lude/aliases.h
tail /usr/include/aliases.h
less /usr/include/aliases.h
more /usr/include/aliases.h

```
### 2. Comparer des fichiers

Prendre deux fichiers texte identiques, en modifier un et lister les différences (diff et cmp).

```
cd ~
mkdir tmp
cd tmp
touch test1
echo HelloWorld > test1
cp test1 test2
ls -la
echo Ceci est un test >> test2
diff test1 test2
cmp test1 test2
```

### 3. Analyser les commandes de base

Que fournissent les commandes suivantes ? Écrire une phrase compréhensible par une personne extérieure du domaine.

```bash
wc -l /etc/passwd # ça va compter les lignes dans le fichier 'passwd'
more /etc/passwd # affiche en mode pagination le fichier 'passwd'
grep li /etc/passwd # affiche les lignes qui contiennent 'li' dedans
grep ^li /etc/passwd # affiche les lignes qui commencent par 'li' dans /etc/passwd
grep 0 /etc/passwd # affiche toute les lignes qui contiennent '0' dans le fichier
cut -d: -f1,4 /etc/passwd # affiche le fichier mais uniquement les sections 1 et 4
more /etc/group # affiche en mode pagination le fichier /etc/group
cut -d: -f1,3 /etc/group # affiche le fichier mais uniquement les sections 1 et 3
```

### 4. Comprendre les pipes et redirections

Exécuter les commandes ci-dessous, comprendre ce qu'il se passe. Écrire une phrase compréhensible par une personne extérieure du domaine.

```bash
cat /etc/passwd | cut -d: -f1 # la première commande affiche le fichier 'passwd' et ensuite la sortie de cette commande on l'utilise dans la deuxième commande et on affiche uniquement la première section (username)
cat /etc/passwd | cut -d: -f1,6 # la première commande affiche le fichier 'passwd' et ensuite la sortie de cette commande on l'utilise dans la deuxième commande et on affiche uniquement la première section (username) et la sixième section (le repertoire home de chaque user)
cat /etc/passwd | cut -d: -f1,6 | less # la meme que celle d'avant mais n'affiche que petit à petit
cat /etc/passwd | cut -d: -f1,4 | grep ':101$' # affiche le nom d'utilisateur et le groudid mais seulement les groupid qui commencent par '101'
cat /etc/group | grep ^user # affiche le fichier group mais seulement les lignes qui commencent par 'user'
cat /etc/passwd | cut -d: -f1,6 | tee ./liste_comptes # ça affiche la section 1 et 6 du fichier passwd et ensuite ça envoie l'output dans un fichier ./liste_comptes
sort < ./liste_comptes > ./liste_comptes.triee # ça trie le fichier liste_compte et ça renvoie l'output trier dans un nouveau fichier nommé ./liste_comptes.triee
cat /etc/passwd | cut -d: -f1,6 | tee ./liste_comptes | less # affiche la section 1 et 6 et ça renvoie l'output dans un fichier ./list_comptes et ensuite ça lis petit a petit l'output de la dernière commmande
cat /etc/passwd | cut -d: -f1,4 >> ./liste_comptes # ça affiche la section 1 et 4 et ça rajoute l'output (ça n'efface pas le fichier) dans le fichier ./liste_comptes
```

### 5. Construire des commandes d'analyse

Écrire/Construire les commandes fournissant:

1. la liste des noms d’utilisateurs locaux avec leurs UID et GID (sur écran et sur fichier)

```bash
cat /etc/passwd | cut -d: -f1,3,4 | tee uid_gid.txt
```

2. la liste des noms des groupes locaux

```bash
cat /etc/group | cut -d: -f1
```

3. le nombre de comptes locaux

```bash
wc -l /etc/passwd
```

4. le nombre de groupes locaux

```bash
wc -l /etc/group
```

### 6. Utiliser la commande find

Exercer la commande `find` dans les 4 cas suivants:

1. recherche selon le nom du fichier

```bash
find . -name "*game*"
```

2. recherche selon le code de permission

```bash
find . -perm 644
```

3. recherche selon la date de modification

```bash
find . -mtime +7
```

4. recherche selon un code de permission et le modifier

```bash
henochrjt@lozan0:~/tmp$ chmod u+rwx,g+rwx,o= test1
henochrjt@lozan0:~/tmp$ ls -la
total 28
drwxr-xr-x 2 henochrjt henochrjt 4096  4 fév 10:09 .
drwxr-xr-x 4 henochrjt henochrjt 4096  9 fév 14:14 ..
-rw-r--r-- 1 henochrjt henochrjt   77  3 fév 21:29 ls-error.log
-rw-r--r-- 1 henochrjt henochrjt   77  3 fév 21:33 ls.log
-rwxrwx--- 1 henochrjt henochrjt   11  4 fév 10:08 test1
-rw-r--r-- 1 henochrjt henochrjt   28  4 fév 10:09 test2
-rw-r--r-- 1 henochrjt henochrjt    3  3 fév 21:21 wc.log
henochrjt@lozan0:~/tmp$ find ./ -perm -660
./test1
henochrjt@lozan0:~/tmp$ find ./ -perm -660 | chmod 0664 test1
henochrjt@lozan0:~/tmp$ ls -la
total 28
drwxr-xr-x 2 henochrjt henochrjt 4096  4 fév 10:09 .
drwxr-xr-x 4 henochrjt henochrjt 4096  9 fév 14:14 ..
-rw-r--r-- 1 henochrjt henochrjt   77  3 fév 21:29 ls-error.log
-rw-r--r-- 1 henochrjt henochrjt   77  3 fév 21:33 ls.log
-rw-rw-r-- 1 henochrjt henochrjt   11  4 fév 10:08 test1
-rw-r--r-- 1 henochrjt henochrjt   28  4 fév 10:09 test2
-rw-r--r-- 1 henochrjt henochrjt    3  3 fév 21:21 wc.log
henochrjt@lozan0:~/tmp$
```
### 7. Compter fichiers et répertoires

1. Créez une instruction qui permet de compter le nombre de fichiers et de répertoires contenus dans un répertoire et ses descendants.

```bash
henochrjt@lozan0:~$ find . | wc -l
20
henochrjt@lozan0:~$
```

2. Modifiez l’instruction sous 1) pour ne compter que les fichiers ordinaires.

```bash
find . -type f | wc -l
```

3. Modifiez l’instruction sous 1) pour ne compter que les répertoires.

```bash
find . -type d | wc -l
```

### 8. Analyse avancée avec tr

Construire la commande qui

- permet de compter le nombre d’utilisateurs qui possède un GID compris entre 100 et 199.

```bash
henochrjt@lozan0:~$ cat /etc/passwd | tr -s ':' | cut -d':' -f4 | awk '$1>=100 && $1<=199' | wc -l
3
henochrjt@lozan0:~$
```

- indique le nom de l’utilisateur et l’IP de sa machine connectés au système sur le terminal 4 (/dev/pts/4)

```bash
who | tr -s ' ' ':' | grep pts/4 | cut -d':' -f1,6
```

**Aide** : utiliser la commande tr (translate) : `tr –s ' ' ':' file(s)`

### 9. Redirection des erreurs

Analyser les commandes suivantes

```bash
find / -user "$USER" # ça recherche à partir de la racine les fichiers qui dans lesquels sont cité mon utilisateur
find / -user "$USER" 2>/dev/null # pareil mais sans les erreurs
```